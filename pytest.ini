[pytest]
# python/ liegt auf dem Importpfad, damit Tests `import db` etc. nutzen können
# (die Skripte importieren modulrelativ, z. B. `from db import get_connection`).
pythonpath = python
testpaths = python/tests
addopts = -ra -q
